// A function that returns true if the memory test has been canceled by user or something external to the test.
type MemoryTestCancelCallback = () => boolean;

export default MemoryTestCancelCallback;